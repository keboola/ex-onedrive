{
  "authorization": {
    "oauth_api": {
      "credentials": {
        "#data": "${OAUTH_DATA}",
        "#appSecret": "${OAUTH_APP_SECRET}",
        "appKey": "${OAUTH_APP_ID}"
      }
    }
  },
  "action": "search",
  "parameters": {
    "workbook": {
      "search": "${FIXTURES_CATALOG::DRIVE::FILE_ONE_SHEET::getPath}"
    }
  }
}
